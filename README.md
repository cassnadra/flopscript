# flopscript
vibe coded floppy leaderboard generator

includes some example db files with cached usernames/topflop info. you can also add your apikey in settings.txt, but it is strictly not necessary and cached names will still be loaded. you can also manually add usernames to the topflop.txt db


todo: find a way to auto gen db info, rn is p easy cus no one else has more than 1 floppy craft i guess lol. 
sort the files better
simple ui maybe, altho i do like the cli interface
custom output options in settings.txt
auto avatar next to usernames (should be rly easy)
store db on server/make accessible thru web interface. i can keep steam API requests handled locally to my machine and make a seperate server side ver to keep my api key safe.
